// oxlint-disable no-empty-file

// //import SimplexNoise from "simplex-noise";
// //var nois = new SimplexNoise(Math.random);

// function noise2(x, y) {
//     const time = performance.now() / 1000;
//     const v = nois.noise3D(x * 20, y * 20, time);

//     //v = Math.pow(2,v);

//     window.loq !== true && (window.loq = true) && console.log(v);
//     return v;
//     //return y*-1 % v;
// }

// export function mainImage(x, y) {
//     throw new Error("Not implemented");
//     /*
//     //return mainImageImpl(new vec2(x,1-y));
//     const iTime = performance.now() / 1000;
//     var rnd = noise2(x,y);
//     return [rnd,rnd,rnd];
//     */
// }

// window.exportMainImage = mainImage;
